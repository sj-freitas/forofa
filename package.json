{
  "name": "for-of-a",
  "version": "1.0.0",
  "description": "Fluent api for lazy iterators in JS",
  "scripts": {
    "test": "jest --no-cache",
    "travis": "jest --no-cache --coverage --coverageReporters=text-lcov | coveralls"
  },
  "author": "Sérgio Freitas",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
